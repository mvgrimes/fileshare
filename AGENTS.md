# AGENTS

- This repo is currently at Sprint 01 (bootstrap, auth/RBAC foundation) and not yet at MVP feature completeness.
- The implementation plan/source of truth for roadmap decisions is `PLAN.md`.
- Session handoff context (what was completed on 2026-05-01 and what is next) is in `data/SESSION_LOG_2026-05-01.md`.
- For migrations, `libsql://` URLs are intentionally not supported by the current Goose SQLite migration path; use a local SQLite file for migration commands.
